# API Keys
OPENAI_API_KEY=sk-your-api-key-here

# Pipeline Settings
ENABLE_CLEANUP=true         # Use GPT-4o-mini to clean up Whisper output (highly recommended)
ENABLE_MEMORY=false         # Build memories from transcripts (experimental, requires 1000+ transcripts)

# Debug Settings (Optional)
DEBUG_MODE=false            # Show detailed output in notifications
