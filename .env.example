# API Keys
OPENAI_API_KEY=sk-your-api-key-here
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Mode Settings (choose ONE - mutually exclusive)
USE_OPENAI_DIRECT=true      # OpenAI gpt-4o-audio-preview (100% reliable, $0.005/use)
USE_OPENROUTER_GEMINI=false # Gemini 2.5 Flash (30% consistency, $0.0001/use)
USE_AUDIO_DIRECT=false      # Voxtral via OpenRouter (experimental)

# Audio Settings (Optional)
# SAMPLE_RATE=16000         # Default: 16000 Hz

# Debug Settings (Optional)
# DEBUG_MODE=false          # Enable verbose logging and save debug audio
